# AI Assistance Log

| Component / Task | Prompt Used | Modifications Made |
|------------------|-------------|---------------------|
| `KdTree.insert()` | "guide step-by-step through implementing recursive `insert()`" | AI provided standard top-down recursive insertion with alternating boolean flags and bounding box tracking. |
| `KdTree.nearest()` | "guide me step-by-step through implementing the recursive nearest() method with distance pruning" | AI provided optimal recursive nearest-neighbor logic with subtree-ordering and distance-squared pruning. |
| `KdTree.contains()` | "implement contains() method step-by-step using recursive helper" | AI implemented public `contains(Point2D p)` with private recursive helper `contains(Node n, Point2D p, boolean useX)` that mirrors insert's dimension alternation (X at even levels, Y at odd levels) and returns true on exact match or false on null node. |
| `KdTree.range()` | "how to implement range search using the intersects() pruning logic" | AI gave the recursive method that checks if a node's rectangle intersects the query rectangle. Added logic to add to list if contained. |
| Architecture Refactor | "refactor Node class into an inner class inside KdTree while maintaining Sedgewick API standard, and handle getter/setter transition" | Changed Node to private static inner class, replaced getters/setters with direct field access, updated imports to edu.princeton.cs.algs4, deleted old custom Point2D/RectHV classes. |
| `PointSET.draw()` and `KdTree.draw()` | "implement visual rendering draw methods with alternating red/blue subdivision lines" | AI implemented PointSET.draw() to render all points in black (radius 0.01). Implemented KdTree.draw() with public method and private recursive helper that draws points in black and subdivision lines with RED for vertical splits (levels 0, 2, 4...) and BLUE for horizontal splits (levels 1, 3, 5...). Lines bounded within their respective bounding rectangles using rect.xmin/xmax/ymin/ymax. |
